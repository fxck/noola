# CLAUDE.md

See @AGENTS.md for how to work on this monorepo — the layout, the Zerops
develop → deploy → PR loop, and the sharp edges (per-service checkouts, doing
git in-container to dodge SSHFS races, `*prod` setup pins, `requireEnv`, and
registering migrations).
